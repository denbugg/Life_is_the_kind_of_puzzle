# Solver step 115: six-arm adjacency consensus is strongly negative

The fixed target-blind selector independently focal-tail96-polished all six
confirmed TASKA arms and selected the whole layout with maximum adjacency
overlap against the other arms. Exact score ties retained the current fusion
layout when possible.

On local32, confirmed fusion `326.78125` pairs / `5.9375` exact fell to
`311.78125` pairs / `1.375` exact. Pair W/T/L was `4/5/23`. The arm-overlap
proxy therefore rewards correlated errors and is closed without held/fresh or
any support/weight sweep. The post-tail six-arm oracle remains material, so a
supervised board-relative selector is a distinct open direction.
