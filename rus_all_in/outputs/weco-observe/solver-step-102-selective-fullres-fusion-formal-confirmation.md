# Solver step 102 — selective + unique-fullres fusion formal confirmation

Parent in both pair and exact Weco Observe runs: step 97.

The unchanged six-arm fusion was run once on a preregistered, signed,
source-disjoint 16-source × 2-draw organizer-train panel. The config and SHA
sidecar existed before candidate code, inference, or scoring. Every target-free
cost matrix, edge/logit roster, strict layout, and provenance record was frozen
before exact references were reconstructed.

- pairs `330.03125 -> 333.12500`, delta **`+3.09375`**, source-cluster CI95
  **`[+0.84375,+5.75000]`**, case W/T/L `7/23/2`;
- recall `0.298941350 -> 0.301743659`;
- exact `0.71875 -> 1.56250`, delta `+0.84375`, CI95
  `[-0.09375,+2.18750]`.

The preregistered pair gate (mean `>=+1.0`, CI95 lower `>=0`) passed. Unique
fullres supply contributed `12.0625` accepted edges and exactly `6.0` true
edges per board at `49.74%` precision. The combined arm won on 9/32 boards.
Exact is positive but remains secondary because its interval crosses zero.
Competition test, output-pixel changes, postprocessing, production, and
submission were not touched.

