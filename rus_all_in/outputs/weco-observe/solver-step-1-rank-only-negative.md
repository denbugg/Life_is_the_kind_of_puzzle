# Solver step 1 — rank-only top5 synchronization control

Panel: established frozen64 engineering panel (already opened; not promotion evidence).

Target-free algorithm committed before scoring:

- top-48 projected edges per axis form rigid translation components;
- all learned-Union top-5 identities contribute reversible rank-weighted component-offset votes on Z24²;
- current Union layout anchors the otherwise free global gauge;
- two coordinate-descent sweeps;
- strict Hungarian projection to all 576 original upright tiles.

Fixed control configuration:

- rank weights: `[1, 0.5, 0.333333333333, 0.25, 0.2]`;
- current-layout anchor weight: `6`;
- synchronization/current costs: `4/1`.

Frozen64 result:

- exact tiles per board: `1.28125` (delta `0`);
- adjacency: `0.1443472600` (delta `+0.0001556839`);
- strict boards: `64/64`;
- mean solver runtime: `0.0202 s/board`.

This does not pass the predeclared exact gate (`+0.25` tile/board). Rank-only soft evidence is retained as a negative control, not promoted.
