# Solver step 98 — selective target500 formal confirmation

Parent in both pair and exact Weco Observe runs: step 92.

The unchanged `solve_selective_vote500` candidate was evaluated once on a
preregistered, current-TASKA-lineage-disjoint 16-source × 2-draw panel selected
from organizer-train `img_006700..img_006999`. The config and SHA sidecar were
frozen before inference; target-free layouts, edge/logit rosters, and
provenance were frozen before reference reconstruction.

- pairs `348.78125 -> 354.28125`, delta **`+5.50000`**, source-cluster
  CI95 **`[+0.81250,+11.31250]`**;
- recall `0.315925045 -> 0.320906929`;
- exact `2.59375 -> 2.81250`, delta `+0.21875`, CI95
  `[-0.12500,+0.62500]`.

The preregistered pair gate (mean `>=+2.0`, CI95 lower `>=0`) passed. This is
confirmation of the pair-oriented solver; exact remains secondary and its
interval crosses zero. Competition test and postprocessing were not used.
