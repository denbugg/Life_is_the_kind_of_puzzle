# HBT continuation candidate dataset

Provisional research checkpoint only. Before upload this directory must contain
the downloaded `hbt_d320_denoised_rgb_sobel_cont.pt`, its matching JSON training
report, and `SHA256SUMS.txt`. The untouched comparator refuses unpatched hashes,
and this artifact is not safe for submission without subsequent gates.
